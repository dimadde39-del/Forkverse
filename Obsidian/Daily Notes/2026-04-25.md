# 2026-04-25

## MonteRun income delay validation

Validated the `income_delay_months` rollout as the fourth/final ParanoidValidator pass.

Scope audited:
- `engine/monte_carlo.py`
- `api/parse.py`
- `api/simulate.py`
- `prompts/parser_v1.txt`
- `components/SimulatorClient.tsx`
- `components/WhatIfControls.tsx`

Canonical constraints applied:
- MonteRun remains a deterministic survival engine, not an AI probability engine.
- LLM parsing can extract `income_delay_months`, but the NumPy core owns all math.
- `income_delay_months` defaults to `0` for old links and old parser payloads.
- Delays greater than the simulation horizon must not crash or index out of bounds.
- Delay handling must stay vectorized, not loop over months inside the simulator.

Audit findings:
- `MonteRunParams` carries `income_delay_months: int = 0`.
- `_simulate_capital_paths(...)` now applies income timing through the `TimeMasks` layer.
- `_build_time_masks(...)` clamps delay to `[0, months]` and builds an `income_active` NumPy mask from `np.arange(months)`.
- Realized income is multiplied by the mask through NumPy broadcasting; no Python loop over months was introduced.
- If `income_delay_months = 3`, income in simulated months `0`, `1`, and `2` is inactive for every path.
- If delay exceeds horizon, all income columns are inactive without out-of-bounds access.
- Parser prompt includes the required instruction: "Extract how many months the user will wait before their first revenue. Default is 0."
- Parser normalization defaults missing delay to `0`, preserves explicit integer delay, and forwards it into `MonteRunParams` and legacy `simulate(...)`.
- `/api/simulate` normalizes `income_delay_months` to a non-negative integer with default `0` before calling the engine.
- What-If UI clamps delay controls to `0..12`, step `1`, with the label `Задержка дохода (мес)`.
- Existing debounced recalculation path is preserved through `useDebouncedSimulation`.

Validation commands:
- `python -m py_compile engine\monte_carlo.py api\parse.py api\simulate.py` passed.
- Focused Python invariant checks passed:
  - omitted delay equals explicit `0`
  - three-month delay keeps first three simulated income months inactive for every path
  - over-range delay clamps to the horizon and does not crash
  - API default is `0` when delay is absent
  - API rejects negative, boolean, fractional, and non-numeric delay values
  - `compute_metrics(...)` remains JSON-serializable and keeps sampled paths bounded
- `npx tsc --noEmit` passed.
- `npm run build` passed on Next.js 16.2.3 / Turbopack.
- `git diff --check` passed for the implementation files, with CRLF normalization warnings only.

Edge cases covered:
- Missing parser delay field defaults to `0`.
- Explicit parser delay propagates through normalized params.
- Clarification state does not ask only because delay is absent.
- Delay greater than horizon is safely clamped to available matrix columns.
- Delay greater than the legacy 24-month simulation horizon is accepted by the Python API and handled in the core.
- Frontend What-If control remains intentionally bounded to `0..12` for user-facing scenario adjustment.

Obsidian report:
- [[01-Projects/MonteRun/Validation/Income Delay Time Masks - 2026-04-25]]

Git commits:
- `e48695b` Validate income delay time masks
- `be480b6` feat: add income delay simulation control

Residual risk:
- No full browser interaction pass was run for the What-If slider; validation covered TypeScript and production build.
- Parser behavior with live LLM output still depends on model adherence, but normalization protects the default and explicit integer propagation paths.

## MonteRun Phase 2 v4-flash Board Mode third validation
ParanoidValidator confirmed the parser-only DeepSeek v4-flash swap, assumptions extraction schema, JSON-object response format, Board Mode assumptions below Verdict, and income-delay stress routed through the existing What-If path.
Validation passed for `python -m py_compile api/parse.py`, `npx tsc --noEmit`, and `git diff --check -- api/parse.py components/SimulatorClient.tsx`. `npm run lint` remains blocked by the current `next lint` script.
## Recent Changes
- [[00-Inbox/Git Log/2026-04-25/195451-6ae3943-docs-update-income-delay-daily-note]] - `6ae3943` docs: update income delay daily note
- [[00-Inbox/Git Log/2026-04-25/195106-e48695b-validate-income-delay-time-masks]] - `e48695b` Validate income delay time masks
- [[00-Inbox/Git Log/2026-04-25/162630-be480b6-feat-add-income-delay-simulation-control]] - `be480b6` feat: add income delay simulation control

## MonteRun Phase 2 Board Mode validation

ParanoidValidator pass for Phase 2 Board Mode.

Scope audited:
- `api/parse.py`
- `components/SimulatorClient.tsx`

Validation notes:
- Parser assumptions stay top-level metadata and are not merged into simulation params.
- Board Mode applies supported stress only through the client What-If path, currently `income_delay_months` for `income_delay` assumptions.
- TypeScript normalization accepts optional/missing assumptions, string assumptions, and parser objects using `text`/`statement`-style fields.
- Deterministic math core files were not edited in this change set.

Validation commands:
- `python -m py_compile api/parse.py` passed.
- `npx tsc --noEmit` passed.
- `npm run lint` did not run lint rules; the current script invokes `next lint`, which exits with `Invalid project directory provided, no such directory: C:\ForkVerse\lint`.

Residual risk:
- Live LLM output can omit assumptions; UI handles that by hiding Board Mode.
- Only `income_delay` stress is actionable in the current Board Mode control path.

## MonteRun parser zero-follow-up and 502 degradation

PromptEnforcer/GracefulDegradation pass for parser loop and timeout handling.

Validation notes:
- Extraction prompt now has a hard zero-follow-up rule and instructs DeepSeek to calculate derived values like `3 clients * $2000 = $6000` instead of asking.
- Extraction runs at `temperature=0.1` and the server normalization applies pessimistic defaults if legacy incomplete params appear.
- Parse and What-If 502/504 failures map to the friendly overload message instead of raw HTTP debug text.

Validation commands:
- `python -m py_compile api/parse.py` passed.
- `npx tsc --noEmit` passed.
- Mocked Playwright parse-502 flow passed.
- `npm run lint` remains blocked by `next lint` resolving `lint` as a project directory.

## Links
- [[MonteRun MOC]]
